<Page
    x:Class="Projekat.Kino.Views.DetaljiFilma"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:Projekat.Kino.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

   
        <Grid>
            <Grid.Background>
                <LinearGradientBrush EndPoint="0.5,1" StartPoint="0.5,0">
                    <GradientStop Color="White" Offset="0.991"/>
                    <GradientStop Color="#FF06082C"/>
                    <GradientStop Color="#FF9CE1F6" Offset="0.745"/>
                </LinearGradientBrush>
            </Grid.Background>
            <Grid.RowDefinitions>
                <RowDefinition Height="*"></RowDefinition>
                <RowDefinition Height="2*"></RowDefinition>
                <RowDefinition Height="*"></RowDefinition>
            </Grid.RowDefinitions>
            <Image x:ConnectionId='1' x:Name="image" Grid.Row="0" HorizontalAlignment="Center" Height="100" Margin="0,5,0,0" VerticalAlignment="Top" Source="logo.jpg" Width="170" Stretch="Fill"/>
        <Grid Grid.Row="1">
            <ListView x:ConnectionId='3' x:Name="detalji" Background="Transparent">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <StackPanel Orientation="Vertical" >
                            <TextBlock Text="{Binding naziv}" Foreground="DarkSlateBlue" FontWeight="Bold" Margin="5,0,0,0"/>
            <TextBlock Text="{Binding godina}" Foreground="DarkSlateBlue" FontStyle="Italic" Margin="5,0,0,0" />
            <TextBlock Text="{Binding zanr}" Foreground="DarkSlateBlue"/>
            <TextBlock Text="{Binding trajanje}"  Foreground="DarkSlateBlue"/>
            <TextBlock Text="{Binding plot}"  Foreground="DarkSlateBlue"/>
            <HyperlinkButton Content="{Binding imdb}" Foreground="Blue" />
                </StackPanel>
                </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Grid>
        <Button x:ConnectionId='2' Content="Nazad"                     Grid.Row="3" VerticalAlignment="Center" HorizontalAlignment="Center" BorderBrush="Blue" BorderThickness="5" Background="Transparent"> </Button>             
                    


           

        </Grid>

</Page>

